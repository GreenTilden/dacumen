{
  "cycle_number": 8,
  "cycle_label": "GOV-08 — governance-thread standalone sprint · fix denormalized deployment_name staleness on reconciliation suggestions (reader fix + backfill) · runs ABOVE the three-sprint cascade",
  "plain_english": {
    "_doc": "Operator-facing summary block. /brief reads this to emit a plain-language header. The governance thread is NOT a cycle — it is a thread of standalone GOV-NN sprints. cycle_number tracks the GOV sprint number (GOV-08 = 8). Re-author this block at each GOV sprint OPEN.",
    "summary": "The governance thread runs ABOVE the three-sprint cascade — not a 4th nephew. It scopes from a health-check sweep, takes ownerless cross-cutting work the cascade structurally can't absorb, and codifies findings to memory + DAcumen. GOV-08 opened 2026-05-15 from a GOV-07 carryforward investigation: the nodemad deployment surfaced as TWO buckets in the reconciliation suggestion grouping ('nodemad' + 'nodemad — N0D3MAD-01 living portable DArnTech platform') but all 5 suggestions share deployment_id c6fa7aa0, and the deployment's current name is 'nodemad — NODEMAD-03 voice-capture-appliance + fleet-template'. The 'duplicate' is a denormalization staleness defect: casey-junior's reconciler snapshots deployment_name into each suggestion at creation time (reconciler.py:329), and never re-attributes when the deployment is renamed. Same structural shape as the GOV-06 L04 append-only-ingester-stale-mapping finding (denormalized data captured at write-time goes stale on upstream change). The inventory confirms the bug is suggestions-only: log_reconciliation_activity stores only deployment_id (clean by design); other deployment_name usages read from the live deployments dict. GOV-08 L01 will land both a reader-side fix (resolve deployment_name from deployment_id at GET /suggestions + GET /suggestions/summary) AND an idempotent backfill pass (POST /api/reconciliation/refresh-deployment-names), then codify the generalized denormalization-staleness-pattern memory.",
    "goals": [
      "L01 — reader fix + backfill + close. PENDING. (1) Add `_resolve_deployment_name(deployment_id)` helper and the `refresh_deployment_names()` backfill function to casey-junior/app/services/reconciler.py (mirrors remap_git_events from L04). (2) Patch GET /api/reconciliation/suggestions + GET /api/reconciliation/suggestions/summary to resolve names at read time so the dashboard stops showing phantom buckets even if storage is stale. (3) Add POST /api/reconciliation/refresh-deployment-names — idempotent, fill-only-style — to actually clean storage. (4) Deploy casey-junior to prod, verify nodemad phantom bucket gone on /suggestions/summary, run the backfill, verify storage updated. (5) Codify the generalized denormalization-staleness pattern (now n=3: L04 git_events, GOV-08 suggestions, and the activity log that AVOIDED it by design — the lesson is about the design choice, not just the fix). Close GOV-08."
    ],
    "decisions_pending_operator": [],
    "proposed_next_action": "Proceed with GOV-08 L01: implement the reader-side helper + endpoint patches + backfill in casey-junior, deploy via `make deploy`, verify on prod (the phantom nodemad bucket should collapse to a single entry with the current 'nodemad — NODEMAD-03 voice-capture-appliance + fleet-template' name), run the backfill to refresh stored names, codify the durable finding, close."
  },
  "pillar": "governance",
  "cycle_mode": "governance-thread-standalone",
  "cycle_mode_notes": "GOV sprints do not pillar-rotate and do not join the cascade. cascade_mode is standalone by design — the three-sprint cascade is capped at 3 (three is the floor AND the ceiling). GOV takes ownerless cross-cutting work, never nephew COLLECT-queue work.",
  "structure": "Health-check sweep scopes the queue → GOV executes the ownerless backlog as a standalone sprint → findings codified to memory + DAcumen",
  "cascade_mode": "standalone — runs ABOVE the three-sprint cascade, not a 4th nephew",
  "charter_version": "GOV-01 charter (inherited — docs/foreman/sprints/GOV-01/charter.md)",
  "status": "open",
  "opened_at": "2026-05-15",
  "closed_at": "",
  "work_locus": "casey-junior (storage + reader endpoints + backfill)",
  "authored_at": "2026-05-15",
  "authored_by": "operator-direct — GOV-08 opened 2026-05-15 during a GOV-07 carryforward investigation. The 'duplicate nodemad deployment' flagged in the GOV-07 post-close addendum turned out to be a denormalization staleness defect: same structural pattern as GOV-06 L04's append-only-ingester-stale-mapping. Operator ratified the recommended scope: reader-side fix + backfill (both, the most thorough option).",
  "stakes": "The /ops dashboard's reconciliation grouping shows phantom deployments whenever a deployment has been renamed since its suggestions were created. Currently visible: 'nodemad' + 'nodemad — N0D3MAD-01 living portable DArnTech platform' both map to deployment_id c6fa7aa0, whose current name is 'nodemad — NODEMAD-03 voice-capture-appliance + fleet-template' — neither bucket name is right. The defect's reach is wider than the single visible symptom: every rename creates a permanent stale entry, and operators reviewing suggestions can't trust which deployment they're triaging. The fix has two parts because each addresses a different failure window: reader-side resolution gives correct display going forward even on stale storage; the backfill pass cleans existing storage so audits, exports, and any non-reader consumer also see truth. Same shape as L04 and the family captured in append-only-ingester-stale-mapping — this loop generalizes the finding into a pattern (denormalization-staleness) and proves the discipline by addressing both the read and write surfaces in the same sprint.",
  "notes": "GOV-08 opened 2026-05-15. Inventory completed at open (L01 first half): denormalized deployment_name lives ONLY in suggestions array of reconciliation.json (reconciler.py:329 snapshots it at creation). log_reconciliation_activity stores only deployment_id (clean by design — when the activity reader needs a name, it joins live). Reader-fix surfaces: GET /api/reconciliation/suggestions (line 39) and GET /api/reconciliation/suggestions/summary (line 63). Backfill mirrors the L04 remap_git_events shape (idempotent, fill-only). Related memory: append-only-ingester-stale-mapping (the precedent, n=2 → n=3 with this loop); fix-without-action-surface-reconciliation (the analogous discipline for action hints); cascade-rc-rename-consumer-runtime-gap (the original family). Casey Junior deployment 09c22323. Memory directory now repo-tracked at governance-thread/memory/ via symlink — this sprint's durable findings land in git history.",
  "sprint_trio": [
    {
      "id": "GOV-08",
      "identity": "governance-thread",
      "role": "governance / standalone-sprint"
    }
  ],
  "carryover_decisions_at_open": {}
}
