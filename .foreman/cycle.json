{
  "cycle_number": 6,
  "cycle_label": "GOV-06 — governance-thread standalone sprint · ops-dashboard doc-health data-path fix + freshness watch + process-health tracking pass · runs ABOVE the three-sprint cascade",
  "plain_english": {
    "_doc": "Operator-facing summary block. /brief reads this to emit a plain-language header. The governance thread is NOT a cycle — it is a thread of standalone GOV-NN sprints. cycle_number tracks the GOV sprint number (GOV-06 = 6). Re-author this block at each GOV sprint OPEN.",
    "summary": "The governance thread runs ABOVE the three-sprint cascade — not a 4th nephew. It scopes from a health-check sweep, takes ownerless cross-cutting work the cascade structurally can't absorb, and codifies findings to memory + DAcumen. GOV-06 CLOSED 2026-05-14, same-day open-to-close, four loops clean. L01 diagnosed the busted /ops doc-health panel (deprecated /pipeline-api/ stranded its consumer) and separated it from the honest process-health 57 signal. L02 fixed the panel by moving it onto the canonical static-artifact path (observatory/data/doc-health-status.json, nightly 23:50 snapshot + deploy); built, deployed, prod-verified. L03 added a freshness watch (doc-health-check.sh, 08:15) that tests both local and prod copies so the artifact can't silently rot. L04 surfaced a deeper finding: the dashboard's evidence_coverage action ('register unmapped repos') misdiagnosed the real cause — casey-junior's backfill is append-only with dedup-by-hash, so existing events never get re-attributed when PROJECT_ENDPOINTS gains deployment_ids; fixed with a remap_git_events() + endpoint, lifted prod evidence_coverage 58→63 (111 events re-mapped across 7 repos). Two durable findings codified (append-only-ingester-stale-mapping, plus the L02 panel-deprecation reaffirms cascade-rc-rename-consumer-runtime-gap). GOV-07 opens from a fresh sweep when next scheduled.",
    "goals": [
      "L01 — ops-dashboard diagnostic sweep. DONE. Root cause of the busted doc-health panel: /pipeline-api/ prod proxy deprecated 2026-05-13 (returns HTTP 404 + deprecation JSON), DocHealth.vue never reconciled — still fetches the dead path → 'Pipeline unavailable'. The doc-health pipeline is healthy but dev-only (dev casey-pipeline :8912 scans local files, returns 47 projects avg 0.697; prod casey-junior has the route but returns empty). process-health 57 confirmed NOT a bug — /api/reconciliation/health honestly reports evidence_coverage 58 (1251/2166 commits mapped) + traceability_depth 83. Fix pattern identified: static artifact under /observatory/data/, the same path telemetry-contract-status.json uses.",
      "L02 — doc-health panel fix. DONE. New darntech/scripts/doc-health-snapshot.sh curls the dev pipeline → writes observatory/data/doc-health-status.json → deploys; nightly 23:50 systemd --user timer installed; DocHealth.vue repointed from the dead /pipeline-api/ to the static artifact (canonical pattern, mirrors telemetry-contract-status.json). No casey-junior change needed — the dev pipeline already serves the data; L02 work_locus collapsed to darntech-only. Built clean, deployed to CT 100, verified on prod: artifact HTTP 200 (47 projects, avg 0.697), dashboard bundle matches the dev build. Commit da94904.",
      "L03 — doc-health freshness watch. DONE. New governance-thread/scripts/doc-health-check.sh tests BOTH copies of the artifact — the local one (snapshot timer working?) and the prod one the panel actually reads (deploy step working?) — and exits non-zero if either is missing, non-JSON, or older than 26h. Daily 08:15 systemd --user timer installed; the unit goes failed where a GOV sweep already greps. Verified: passes clean on the L02-fresh artifact, and the negative test (missing artifact → rc=1) confirms it actually fires.",
      "L04 — process-health pass + GOV-06 close. DONE. The dashboard's action ('register unmapped repos in PROJECT_ENDPOINTS') misdiagnosed the gap: 24/28 entries already had deployment_id; the real cause was casey-junior's backfill_git_history() dedups by hash and skips existing commits, so events created before a repo got deployment_id keep their old null attribution forever. Fix: new remap_git_events() + POST /api/reconciliation/remap endpoint (fill-only, idempotent) + vocola deployment_id added; deployed casey-junior to prod and ran the remap — 111 events re-mapped across 7 repos. evidence_coverage lifted 58→63 (1257→1368 / 2176), composite 57→58. Durable finding codified to memory/append-only-ingester-stale-mapping.md. GOV-06 closed."
    ],
    "decisions_pending_operator": [],
    "proposed_next_action": "GOV-06 is CLOSED — all four loops clean, no carryover, no escalations. Doc-health panel fixed + freshness-watched; process-health pass surfaced a structural ingester-drift finding and fixed it (evidence_coverage 58→63 on prod). To open GOV-07: run a fresh health-check sweep FIRST per the operating model. Two new standing instruments to include in the sweep: doc-health-check.timer (08:15) — a red checker means the doc-health artifact has rotted on local or prod; observatory-doc-health-snapshot.timer (23:50) — should fire green nightly. Known dormant items (not carryover, just context): cycle-09 WS-B2 Nextcloud content-sync substrate (still dormant); the remaining 808 unmapped git events on prod (repos genuinely outside PROJECT_ENDPOINTS — likely per-project follow-up rather than GOV-shaped)."
  },
  "pillar": "governance",
  "cycle_mode": "governance-thread-standalone",
  "cycle_mode_notes": "GOV sprints do not pillar-rotate and do not join the cascade. cascade_mode is standalone by design — the three-sprint cascade is capped at 3 (three is the floor AND the ceiling). GOV takes ownerless cross-cutting work, never nephew COLLECT-queue work.",
  "structure": "Health-check sweep scopes the queue → GOV executes the ownerless backlog as a standalone sprint → findings codified to memory + DAcumen",
  "cascade_mode": "standalone — runs ABOVE the three-sprint cascade, not a 4th nephew",
  "charter_version": "GOV-01 charter (inherited — docs/foreman/sprints/GOV-01/charter.md)",
  "status": "closed",
  "opened_at": "2026-05-14",
  "closed_at": "2026-05-14",
  "work_locus": "cross-BU (darntech · casey-junior)",
  "authored_at": "2026-05-14",
  "authored_by": "operator-direct — GOV-06 opened immediately after GOV-05 closed. Operator handed GOV a targeted symptom (the /ops doc-health panel is busted, process-health stuck at 57); L01's diagnostic sweep ran same-session and found the root cause — a deprecation that stranded a consumer. Operator-confirmed scope: doc-health data-path fix + freshness watch + process-health tracking pass.",
  "stakes": "The /ops dashboard is the single source of truth for project status — a busted doc-health panel means a core ops surface is silently lying. The root cause is the cascade-rc-rename-consumer-runtime-gap pattern again: a deprecation (the /pipeline-api/ prod proxy, 2026-05-13) was not 'landed' because its consumer (DocHealth.vue) was never reconciled. GOV-06 fixes the data path onto the canonical static-artifact pattern AND adds a freshness watch — so the panel both works now and reports its own failure if the artifact rots, rather than silently showing stale or empty data.",
  "notes": "GOV-06 opened 2026-05-14, operator-directed, work_locus darntech + casey-junior. Scope: (1) doc-health pipeline writes observatory/data/doc-health-status.json + DocHealth.vue reads the static artifact (canonical pattern, mirrors telemetry-contract-status.json / TelemetryContractsCard.vue); (2) standing freshness watch for the artifact; (3) process-health tracking pass — register unmapped repos in PROJECT_ENDPOINTS to lift evidence_coverage (1251/2166 commits mapped at open). Key durable-finding candidate: deprecating a prod proxy without reconciling its dashboard consumers strands the consumer silently — the deprecation response was a clean 404+JSON, so nothing alerted; the dashboard just showed 'unavailable'. Deploy to dev AND prod per homelab rules. Governance-thread workspace remains a checkout of dacumen.git; Casey Junior deployment 09c22323.",
  "sprint_trio": [
    {
      "id": "GOV-06",
      "identity": "governance-thread",
      "role": "governance / standalone-sprint"
    }
  ],
  "carryover_decisions_at_open": {}
}
